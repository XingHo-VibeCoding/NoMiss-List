# NoMiss List · 产品需求文档（PRD）

> 文档：PRD.md ｜ 版本：v2（含一次 AI 自检修复）｜ 日期：2026-09-19
> 阶段：Vibe Coding 五步工作流第 ② 步「写 PRD」
> 上游文档：research.md（需求研究）｜ 下一步：Day 5 技术选型

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 产品名 | NoMiss List |
| 一句话定位 | 不是帮你「记住要做什么」，而是告诉你「**现在该做什么**」 |
| 目标用户 | 每天有很多项任务的人（不限学生党 / 工作党），对事项安排有需求的群体 |
| 产品立场 | 站在用户这一边：**不做监工，做调度员**。不提醒你还有多少没做，只告诉你接下来做什么 |
| 本版本（MVP）目标 | 在浏览器里跑通一条完整闭环：录入课表 → 添加任务 → 看到「今天该做什么」的建议 → 完成（可分段）→ 建议自动更新 |

### 三条产品原则（贯穿全文，冲突时以此为准）

| # | 原则 | 具体含义 |
|---|---|---|
| P-A | **用户有自主权** | 任何自动行为都必须可见、可改、可撤销；系统**不擅自改动**用户已确定的时间安排 |
| P-B | **AI 随时补位** | 用户不指定时间时，系统主动把任务排进空档，并在每次打开时刷新建议 |
| P-C | **不施压** | 不用红色计数、不用逾期标记、不用积分连击、不主动弹窗；**改用算法规避压力**（逾期任务的紧迫度天然为负，会自动排到最前，无需标红） |

---

## 二、要解决的问题

| 编号 | 痛点 | 来源 |
|---|---|---|
| P1 | **远期遗忘**：只显示今天/明天的清单，会让人忘掉一周后的安排 | 本人使用经验 |
| P2 | **冲突不可见**：任务一多，靠自己比对时间看不出哪些撞了 | 本人使用经验 |
| P3 | **焦虑而非方案**：现有工具只强调「你还有多少没做」，不给解决办法 | 本人使用经验 + 外部研究 |

**补充依据**：外部研究显示，用户放弃待办类 App 的主因是复杂度、决策疲劳、愧疚感、功能蔓延与设置成本——核心不是「记不住」，而是「不敢打开」。详见 research.md 第五节。

---

## 三、本期目标与成功标准

**本期要达成**：

1. 一个网页，打开就能用，无需注册、无需配置
2. 能录入每周固定占用（课表），并自动重复
3. 能快速添加任务，且只需填最少信息
4. 能看出一周的时间分布，以及**哪些时段撞了**
5. 每次打开都能看到一条「现在做什么」的建议，并知道为什么是它
6. 长任务做不完一个整段时，能被**分段推进**而不是被迫拖延
7. 没能按时完成的任务，能被**重新安排**（经用户确认）

**本期不追求**：多设备同步、账号体系、消息推送、AI 能力、移动端 App、商业化。

**成功标准（自我验收）**：

- 自己使用一周，不需要看说明书就能完成「加任务 → 看懂建议 → 打勾」
- 找 1–3 位同学试用，第一屏能自己看懂「现在该做什么」，无需口头解释
- 界面文案中不出现任何施压性词汇（详见第 8 节 F11）

---

## 四、功能范围（本期要做）

### F1 一周视图

- **做什么**：展示**从今天起连续 7 天**的时间分布与负荷（含今天）
- **入口**：首页中部
- **关键行为**：
  - 每天显示占用时长与负荷比例
  - **过载判定**：当日已占用时长（固定占用 + 已排任务）÷ 当日可用时长 ≥ 80% 时，该天用暖色标出
  - 当日可用时长 = `dayEnd` − `dayStart`

### F2 课表（每周固定占用）

- **做什么**：录入每周重复的固定安排（如「周三 08:00–09:40 高等数学」）
- **入口**：页面④「课表与设置」
- **关键行为**：一次录入、每周自动重复；可编辑、可删除；结束时间早于开始时间时拒绝保存并提示

### F3 冲突提示 ★核心差异

- **做什么**：当两个事项时间重叠时，**主动标出撞了的那一段**
- **入口**：首页「本周大局」区块
- **判定对象**：**所有带时间区间的事项**，即「固定占用（课表）」与「已排任务」**互相之间、各自之间**全部两两比对
- **判定规则**：两个时间段**有交集**即算冲突；一段的结束时间**正好等于**另一段的开始时间**不算**冲突
- **关键行为**：
  - 用虚线圈出重叠时段，并给出文字提示
  - 能看出相撞的是哪两件
  - 任一方时间被修改后，标记**立即**更新
  - **系统补位时必须主动避开冲突**：自动排入的时段不得与任何既有事项（课表或已排任务）重叠；相撞只可能由用户手动指定时段或课表本身产生

### F4 快速添加任务

- **做什么**：以最低成本记下一件事
- **入口**：首页右下角「＋ 添加任务」
- **关键行为**：
  - 只填任务名即可保存；截止时间、预计用时、执行时段**均为选填**
  - 预计用时不填时取 `defaultEstimate`（默认 30 分钟）
  - 不填执行时段的任务进入「没安排清单」，由系统补位（P-B）
  - 保存后立即出现在对应日期

### F5 今日建议 ★核心差异

- **做什么**：给出「现在做什么」的**唯一一条**建议，并说明理由
- **入口**：首页最顶部（打开即见，无需点击）
- **关键行为**：
  - 当今天有空档且有未完成任务时，显示且仅显示 **1 条**建议
  - 建议内容包含三要素：做什么、在哪段空档做、为什么是它（理由含截止时间与预计用时）
  - 该任务完成后（**或分段推进一段结束时**），建议自动刷新为下一条 / 下一段
  - 处于「待重新安排」状态的任务**不参与**本建议——先在「待重新安排」区确认改期，回到待办后才重新参与
  - 今天没有空档或没有任务时，显示明确的状态文字（不留空白，也不催促）
- **空档定义**：在 `dayStart`–`dayEnd` 内，扣除当日固定占用与已排任务后剩余的连续时间区间；**且只取开始时间晚于当前时刻的部分**（已过去的空档不再作为建议依据）
- **算法（规则版，非 AI）**：
  1. 算出今天**当前时刻之后**的所有空档（起止与时长）
  2. 计算每个未完成任务的紧迫度 = （截止时间 − 当前时刻）÷ `remainingMinutes`（**逾期时为负数**，天然排最前）
  3. 按紧迫度升序，取第一个时长放得下的任务作为建议
  4. 若最紧急任务的 `remainingMinutes` 大于所有空档时长，仍给出建议，形式为**分段**：「先做 X 分钟，还剩 Y 分钟」（见 F8）
  5. 若今天完全没有空档 → 显示状态文字，并给出**最早可行的整段或分段安排时间**
- **补位规则（P-A 的体现）**：系统补位只处理「没安排清单」里的任务，**绝不擅自移动用户已确定时段的事项**；若需要动，必须先询问用户（见 F9）

### F6 本地存储

- **做什么**：所有数据存在浏览器本地，不上传服务器
- **关键行为**：关闭浏览器重开数据仍在；**全程不发送任何网络请求，断网可用**

### F7 轻量反馈（L1 + L2）

- **L1 动作级**：任务打勾后状态立即变为已完成，首页建议自动翻到下一条
- **L2 当日结算级**：当天已排任务全部完成时，显示**事实陈述**——今天安排了 N 件、完成 N 件、今天还剩多少自由时间
  - 自由时间 = 今天剩余空档总时长 − **今天已排入但尚未完成的任务**的剩余用时之和（结果不小于 0；**不计入**未排入今天的任务）
- **明确不做**：L3 周复盘（延后）、积分、等级、连续天数、排名、养成类玩法

### F8 分段推进（本次新增）

- **做什么**：任务做不完一个整段时，允许跨多个空档推进，**任务本身始终是一条，不生成子任务**
- **触发场景**：建议时段短于任务的 `remainingMinutes`
- **关键行为**：
  - 建议文案显示为：「先做 X 分钟，还剩 Y 分钟（共需 Z）」
  - 一个段落结束时，系统只问一句：**「做完了吗？」**
    - 选「做完了」→ 任务 `status = done`
    - 选「还没，继续」→ `remainingMinutes` 减去刚完成的段时长，并自动排进下一个空档
  - **直接打勾视为完成**，不再询问「做完了吗？」；只有走到「本段时长用尽」这一步才询问
  - **续排边界**：由系统补位排出的时段，可自动续排到下一个空档；**由用户手动指定的时段，续排前必须先询问用户**（遵守 P-A）
  - 同一任务可以有多段时段；每段的时段被独立记录（见第七节 `scheduledSegments`）
  - **不做真实计时、不记录用户实际花了多久**，只依据用户的一次点击来扣减

### F9 待重新安排（本次新增）

- **做什么**：截止时间已过且未完成的任务，进入重新安排流程
- **入口**：首页「待重新安排」区（仅在该区有内容时出现）
- **关键行为**：
  - 任务 `status` 变为 `needsReschedule`
  - 系统给出一条建议：「『实验报告』原定 9月20日 12:00 截止，放到今天 19:00–20:00？」
  - **必须由用户确认**后才写入新的执行时段，`status` 回到 `todo`
  - 建议时段**由 F5 的同一套算法生成**（紧迫度 + 空档），不另建一套逻辑
  - 在用户确认之前，该任务**不参与** F5 的今日建议，避免同一件事在两处重复出现
  - **不做自动顺延**；界面不出现「逾期」字样，不使用红色
- **优先级体现**：无需标红——逾期任务的紧迫度计算为负数，在 F5 中天然排到最前

### F10 偏好设置（本次新增）

- **做什么**：让用户可修改系统默认值
- **入口**：页面④「课表与设置」中的「偏好设置」区块
- **可设置项**：每天可用时间起点 `dayStart`、终点 `dayEnd`、新任务默认预计用时 `defaultEstimate`
- **默认值**：`dayStart` 08:00、`dayEnd` 22:00、`defaultEstimate` 30 分钟；首次使用即为默认值，无需任何配置

---

## 五、明确不做（本期排除）

| 不做的事 | 理由 |
|---|---|
| 消息推送 / 响铃提醒 | 与「不施压」原则直接冲突（P-C） |
| 积分 / 等级 / 连续天数 / 养成玩法（种树、养宠物） | 驱动力是**损失厌恶**（断签、掉分、宠物饿死），本质仍是施压 |
| **自动挪动用户已定的事项** | 与「用户有自主权」冲突（P-A）；本期一律改为**先问用户** |
| **逾期任务自动顺延（不告知）** | 用户会产生失控感；本期一律**等用户确认** |
| 把长任务**真拆成子任务** | 会让清单膨胀，踩中「功能蔓延 + 决策疲劳」两个死穴 |
| 真实耗时统计（开始/结束计时） | 交互太重；分段推进靠一次点击扣减即可 |
| L3 周复盘 | 延后，等 MVP 跑通后再评估 |
| 账号注册 / 登录 | 28 天做不完；会把「打开就有用」变成「先注册再说」 |
| 多人协作 / 共享 | 先服务「一个人」的场景 |
| AI 功能 | 规则能算的事不交给黑箱；也避免密钥与成本问题 |
| 手机 App | 只做网页，手机浏览器可用即可 |
| 月视图 / 年视图 | 一周视图已覆盖主要场景 |
| 拖拽调整时间 | 先验证「自动建议」是否被接受，再考虑手动微调 |
| 番茄钟 / 习惯打卡 | 竞品已做到极致，不在该赛道竞争 |
| 标签 / 优先级 / 项目分类 | 正是「决策疲劳」的来源，与零配置原则冲突 |
| 自然语言时间识别 | 本期用点选代替，降低实现风险 |

### 本节补充：为什么砍掉它们（Day 4 每日一问 · 本人回答）

**问题**：你在 PRD 里砍掉了什么功能？为什么砍它？

**本人回答（原话，未作改写）**：

> 「我觉得一个就是去掉了积分制度的设计，这其实是会引起用户的疲惫的，另外这个长任务分短任务的想法，也是有弊端，但是你自动减掉时间，这就很灵活了，我就觉得很不一样。」

**这段回答对应的文档位置（整理，非原话）**：

| 回答中的内容 | 对应本文档的位置 |
|---|---|
| 去掉积分制度设计，因为会引起用户疲惫 | 第五节「积分 / 等级 / 连续天数 / 养成玩法」一项；F7 的「明确不做」 |
| 长任务分短任务有弊端 | 第五节「把长任务真拆成子任务」一项 |
| 但"自动减掉时间"很灵活 | F8 分段推进：任务仍是一条，只让 `remainingMinutes` 递减——**不做子任务、不做真实计时** |

**这段判断的产品意义**：砍掉积分是为了不制造疲惫；砍掉"拆子任务"是为了不制造混乱；而"自动减时间"是用最少的交互（一次点击）换来碎片时间也能推进长任务。三者指向同一条原则——**减少用户要做的决定，而不是减少功能**。

---

## 六、页面与交互

### ① 首页（第一屏）

| 位置 | 内容 | 交互 |
|---|---|---|
| 最顶部 | **今天的建议**：唯一一条建议 + 理由 + 依据的空档；分段时显示「先做 X 分钟，还剩 Y」 | 完成后自动翻到下一条 |
| 中部 | **本周大局**（即一周视图）：从今天起 7 天负荷；过载日暖色；**冲突时段虚线圈出** | 点击某天可展开当天安排 |
| 中部下方 | **待重新安排**：逾期未完成的任务 + 系统给的建议时段（仅有内容时出现） | 一键确认改期 |
| 底部 | **没安排清单**：中性列出未排入时间表的任务 | 每项右侧「放进今天」 |
| 全局 | 右下角「＋ 添加任务」入口 | — |

**冷启动状态（首次打开、无课表无任务）**：显示引导文案（如「先把你每周的课录进来，或者直接加一件事」）+ 两个入口按钮，**不留空白页**。

### ② 添加 / 编辑任务

- 字段：任务名（必填）、截止时间（选填）、预计用时（选填，默认 30 分钟）、执行时段（选填）
- 按钮：保存、取消；编辑时增加删除
- 保存后：立即出现在对应日期；若与既有事项冲突，该时段立即被圈出

### ③ 任务详情

- 显示：名称、截止时间、预计用时、剩余用时、执行时段、状态、创建时间
- 动作：编辑、删除、标记完成

### ④ 课表与设置

- **区块 A 每周固定占用**：录入（星期几 + 开始时间 + 结束时间 + 名称）、列表、编辑、删除；每周自动重复
- **区块 B 偏好设置**：`dayStart`、`dayEnd`、`defaultEstimate`（见 F10）

### 贯穿所有页面的交互原则

1. 任何页面都允许「什么都不填就退出」，不做拦截
2. 不弹骚扰性确认框（仅删除、以及 F9 改期时确认）
3. 界面不出现红色数字计数（绝不显示「你有 N 项未完成」）
4. 不做主动弹窗与推送

---

## 七、数据字段

### 任务 Task

| 字段 | 类型 | 必填 | 说明 | 举例 |
|---|---|---|---|---|
| `id` | 字符串 | 是 | 系统生成的唯一编号 | `t_1726732800` |
| `title` | 字符串 | 是 | 任务名 | 实验报告 |
| `dueAt` | 日期时间 | 否 | 截止时间 | 2026-09-20 12:00 |
| `estimateMinutes` | 数字 | 否 | 预计用时（分钟），默认取 `defaultEstimate` | 180 |
| `remainingMinutes` | 数字 | 是 | **剩余用时**，创建时等于 `estimateMinutes`；分段推进后递减 | 90 |
| `scheduledSegments` | 数组 | 否 | **已排定时段列表**，每项含 `start` / `end`；一个任务可跨多个空档（分段推进） | `[{start: 09-19 12:00, end: 13:30}, {start: 09-19 16:00, end: 17:30}]` |
| `status` | 枚举 | 是 | `todo` / `done` / `needsReschedule` | todo |
| `createdAt` | 日期时间 | 是 | 创建时间 | — |
| `completedAt` | 日期时间 | 否 | 完成时间 | — |

**说明**：`scheduledSegments` 是 F1 负荷计算、F3 冲突判定、F5 空档计算的共同依据（**用数组而非单一对起止时间，因为 F8 分段推进会让一个任务占用多个空档**）；`remainingMinutes` 是 F8 分段推进的依据。二者缺一，对应功能无法实现。

### 固定占用 Block（课表）

| 字段 | 类型 | 必填 | 说明 | 举例 |
|---|---|---|---|---|
| `id` | 字符串 | 是 | 唯一编号 | `b_01` |
| `title` | 字符串 | 是 | 课程 / 事项名 | 高等数学 |
| `weekday` | 数字 | 是 | 星期几（1–7） | 3 |
| `startTime` | 时间 | 是 | 开始时间 | 08:00 |
| `endTime` | 时间 | 是 | 结束时间 | 09:40 |

### 设置 Settings

| 字段 | 类型 | 必填 | 默认 | 说明 |
|---|---|---|---|---|
| `dayStart` | 时间 | 是 | 08:00 | 每天可用时间起点 |
| `dayEnd` | 时间 | 是 | 22:00 | 每天可用时间终点 |
| `defaultEstimate` | 数字 | 是 | 30 | 新任务默认预计用时（分钟） |

### 存储

- 位置：浏览器 localStorage
- 键名：`nomiss.tasks`、`nomiss.blocks`、`nomiss.settings`
- 不做服务器、不做账号、不发网络请求

---

## 八、验收标准 ★

> 判断「做完了没有」的唯一依据。每条都必须可被肉眼或操作检验。

### F1 一周视图

| # | 验收标准 |
|---|---|
| 1.1 | 打开首页，能看到从**今天起连续 7 天**（含今天） |
| 1.2 | 有安排的那一天，能看出占用了多少（时长或负荷比例） |
| 1.3 | 某天已占用时长占当日可用时长 ≥ 80% 时，该天显示为过载样式 |
| 1.4 | 刷新页面后仍显示同样的 7 天，数据不丢 |

### F2 课表

| # | 验收标准 |
|---|---|
| 2.1 | 录入「周三 08:00–09:40 高等数学」后，本周及以后每个周三都显示该占用 |
| 2.2 | 删除该条后，所有周三都不再显示 |
| 2.3 | 录入结束时间早于开始时间的记录时，系统拒绝保存并给出提示 |

### F3 冲突提示

| # | 验收标准 |
|---|---|
| 3.1 | 当**任意两个带时间区间的事项**（课表或已排任务）有交集时，该时段被圈出并附带文字提示 |
| 3.2 | 从提示能看出相撞的是哪两件 |
| 3.3 | 把其中一件改到不重叠后，冲突标记**立即**消失 |
| 3.4 | 一件事的结束时间正好等于另一件事的开始时间时，**不**判定为冲突 |
| 3.5 | 课表与课表相撞、课表与任务相撞、任务与任务相撞，三种情况都能被识别 |
| 3.6 | 由系统自动补位排出的时段，不会与任何既有事项重叠（即系统自己不会制造冲突） |

### F4 快速添加任务

| # | 验收标准 |
|---|---|
| 4.1 | 只填任务名即可保存成功，其余字段自动取默认值 |
| 4.2 | 保存后任务立即出现在对应日期上 |
| 4.3 | 不填执行时段的任务出现在「没安排清单」中 |
| 4.4 | 输入很长的任务名不会破坏页面排版 |

### F5 今日建议

| # | 验收标准 |
|---|---|
| 5.1 | 当今天有空档且有未完成任务时，首页顶部显示建议，且**只有 1 条** |
| 5.2 | 建议必须包含三要素：做什么、在哪段空档、为什么是它（理由含截止时间与预计用时） |
| 5.3 | 完成该任务后，建议自动切换为下一条 |
| 5.4 | 今天没有空档或没有任务时，显示明确的状态文字，既不空白也不催促 |
| 5.5 | 今天恰好完全没有空档时，明确提示并给出最早可行的安排时间 |
| 5.6 | **已过去的时段不作为空档**：下午打开时，建议不会落在上午 |
| 5.7 | 已完成任务不会再次出现在建议中 |
| 5.8 | 处于「待重新安排」状态的任务不会出现在今日建议中 |

### F6 本地存储

| # | 验收标准 |
|---|---|
| 6.1 | 关闭浏览器再打开，任务与课表数据仍在 |
| 6.2 | 刷新页面后数据不重复、不丢失 |
| 6.3 | 断开网络后，所有功能仍可正常使用（不发送任何网络请求） |

### F7 轻量反馈

| # | 验收标准 |
|---|---|
| 7.1 | 打勾后任务状态立即变为已完成，无需刷新 |
| 7.2 | 当天已排任务全部完成时，显示事实陈述（安排了 N 件、完成 N 件、剩余自由时间） |
| 7.3 | 该陈述中不出现「加油」「应该」「坚持」「失败」等词 |

### F8 分段推进

| # | 验收标准 |
|---|---|
| 8.1 | 当空档时长小于任务剩余用时，建议显示为「先做 X 分钟，还剩 Y 分钟」 |
| 8.2 | 一个段落结束后，系统只出现「做完了吗？」一次询问，选项为「做完了」「还没，继续」 |
| 8.3 | 选「还没，继续」后，任务剩余用时减少相应时长，且被排进下一个空档 |
| 8.4 | 选「做完了」后，任务状态变为已完成 |
| 8.5 | 全过程**不会**为同一任务生成任何子任务或额外清单项 |
| 8.6 | 直接打勾即视为完成，系统**不再**弹出「做完了吗？」的询问 |
| 8.7 | 系统补位排出的时段可以自动续排；用户手动指定的时段在续排前会先询问 |
| 8.8 | 分段结束后，首页建议立即刷新为下一段或下一条 |

### F9 待重新安排

| # | 验收标准 |
|---|---|
| 9.1 | 截止时间已过且未完成的任务，出现在首页「待重新安排」区 |
| 9.2 | 该区为每个此类任务给出建议时段，且**必须由用户确认**后才写入 |
| 9.3 | 用户未确认时，任务的执行时段不会被系统自动改动 |
| 9.4 | 确认后任务回到待办状态，并带有新的执行时段 |
| 9.5 | 界面上不出现「逾期」字样，也不使用红色标注 |
| 9.6 | 用户未确认时，该任务**不出现**在「今日建议」中 |
| 9.7 | 该区给出的建议时段与「今日建议」采用同一套算法，不出现两套互相矛盾的结果 |
| 9.8 | 逾期任务的优先体现为算法排序（紧迫度为负），界面上不出现「逾期」字样或红色标记 |

### F10 偏好设置

| # | 验收标准 |
|---|---|
| 10.1 | 可在页面④修改每天的可用时间起止，保存后 F1 与 F5 的计算随之变化 |
| 10.2 | 可修改新任务默认预计用时，新建任务时该值生效 |
| 10.3 | 首次使用时，三项默认值分别为 08:00 / 22:00 / 30 分钟，无需任何配置 |

### F11 防焦虑底线（全局）

| # | 验收标准 |
|---|---|
| 11.1 | 界面任意位置都不出现「未完成数量」的红色计数 |
| 11.2 | 不出现积分、等级、连续天数、排名 |
| 11.3 | 不出现任何主动弹出的提醒、弹窗或推送 |
| 11.4 | 文案中不出现「逾期」「失败」等指责性词汇 |
| 11.5 | 系统不会在用户未确认的情况下改动其已确定的安排 |

### F12 冷启动与空状态

| # | 验收标准 |
|---|---|
| 12.1 | 首次打开（无课表无任务）时，显示引导文案与明确入口，不留空白页 |
| 12.2 | 各区块在无数据时均显示说明性文字，不显示 0 或空框 |

---

## 九、里程碑（28 天）

| 时间 | 做什么 |
|---|---|
| Day 1–2 | 环境与仓库 ✅ |
| Day 3 | 需求研究 ✅ |
| Day 4 | PRD（本文档） |
| Day 5 | 技术选型 |
| Day 6 | 页面设计与原型 |
| Day 7 起 | 编写 MVP 代码，按第 8 节验收标准逐条自测 |
| 收尾 | 自测通过 → 找同学试用 → 上线 |

> 具体日期以课程当日清单为准，此处仅描述顺序。

---

## 十、风险与待确认

| # | 风险 / 待确认 | 影响 | 计划 |
|---|---|---|---|
| 1 | 一周视图在手机小屏上的可用性 | 手机上可能挤成一团 | Day 6 设计阶段优先在手机宽度下验证 |
| 2 | 「今日建议」的算法是否真被接受 | 若建议不合理，核心价值不成立 | MVP 后自己用一周 + 找同学试用收集反馈 |
| 3 | 预计用时不填时按 30 分钟计算 | 会导致紧迫度偏差 | 界面上轻提示可修改，但不强制 |
| 4 | 冲突判定规则（有交集即冲突）是否合适 | 可能误报 | 先用简单规则，使用中再调整 |
| 5 | localStorage 清缓存即丢数据 | 数据可能丢失 | MVP 阶段接受；后续可加导出功能 |
| 6 | 分段推进可能让"做完了吗"的询问变频繁 | 打扰感 | 仅在段落结束时询问一次；若频繁则改为默认继续、可手动标完成 |
| 7 | 空档计算依赖用户设置的可用时间范围 | 设置不准则空档不准 | 提供默认值，可在页面④修改 |
| 8 | F8 与 F9 都是本期新增，工作量高于原计划 | 可能挤占打磨时间 | 优先保证 F1/F3/F5；F8/F9 若来不及按降级方案处理（见下） |
| 9 | 目标用户规模与付费意愿 | 完全未验证 | 本期不做商业化，不处理 |

**降级方案（若时间不足）**：先完成 首页、任务详情、以及「添加任务」这一项写入功能，其余（F8 分段推进、F9 待重新安排、F10 偏好设置）列为后续。
